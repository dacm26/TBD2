<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :customer_category_id %><br>
    <%= f.select :customer_category_id, options_for_select(CustomerCategory.all.map{|s|[s.name,s.id]}) %>
  </div>
  <div class="field">
    <%= f.label :seat_category_id %><br>
    <%= f.select :seat_category_id, options_for_select(SeatCategory.all.map{|s|[s.name,s.id]}) %>
  </div>
  <div class="field">
    <%= f.label :customer_id %><br>
    <%= f.select :customer_id, options_for_select(Customer.all.map{|s|[s.name,s.id]}) %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
